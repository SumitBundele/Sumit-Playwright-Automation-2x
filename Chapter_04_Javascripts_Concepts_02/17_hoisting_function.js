function getUserStatus(userId) {
    //var status_code = "Inactive"; --done automatically by JS engine with undefined
    console.log(status_code);
    var status_code = "Active";
    console.log(status_code);
}

getUserStatus();
/*   if (userId === 1) {
       return "Active";
   } else {
       return "Inactive";
   }
   */