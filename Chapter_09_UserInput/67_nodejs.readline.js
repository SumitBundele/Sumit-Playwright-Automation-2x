/*const redline = require('readline');

const rl = redline.createInterface({
    input: process.stdin,
    output: process.stdout
});
rl.question
    ("What is your name? ", function (name) {
        console.log("Hello " + name);
        rl.close();
    }

    */

// We will never use this code in our automation
//  but it is good to know how to take user input in NodeJS