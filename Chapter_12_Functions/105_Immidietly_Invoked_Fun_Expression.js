(function name() {
    console.log("hi sumit");
})();

//This is an example of an Immediately Invoked Function Expression (IIFE) in JavaScript.
// An IIFE is a function that is defined and executed immediately after its creation.
// It is often used to create a new scope and avoid polluting the global namespace.


//it call sitself

(function name() {
    //Playright code here -this is not a reusalbe function,
    //  it is just a block of code that will be executed immediately.
})();

//most of iife function do not have a name,
// they are anonymous functions,
// but you can give them a name if you want to.