//Enum is used to define a set of named constants.
// It can be used to represent a collection of related values in a more readable and maintainable way.
//Enums can be numeric or string-based, and they can also have computed values.

//Numeric Enum

enum TestStatus {
    Pass = "PASS",
    fail = "FAIL",
    skip = "SKIP",
    pending = "PENDING",
    blocked = "BLOCKED"
}
