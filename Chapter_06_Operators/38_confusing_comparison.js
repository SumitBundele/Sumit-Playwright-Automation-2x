// We will always user === only to compare values in JavaScript. We will never use == to compare values in JavaScript.

//== (Equal to) operator can lead to unexpected results due to type coercion. It converts the operands to the same type before making the comparison, which can lead to confusing outcomes. For example: